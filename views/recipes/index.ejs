<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Recipes</title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <h1>My Recipes</h1>
  <a href ="/recipes/new">Add a Recipe</a>

  <ul>
    <% recipes.forEach((recipe) => { %>
      <li>
        A listing in <%= recipe.name %> <%= recipe.ingredients %> owned by <%= recipe.owner.username %>
        <a href="/recipes/<%= recipe._id %>">View more details</a>
      </li>
    <% }) %>
  </ul>



</body>
</html>